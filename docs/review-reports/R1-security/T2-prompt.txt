你是 API 安全审查专家。请扫描本项目所有 NestJS Controller，逐项检查：

## 审查清单
1. 所有接口是否要求 Authorization: Bearer {token}（列出未保护的端点）
2. 频率限制是否配置（@nestjs/throttler 100次/分钟）
3. TypeORM 是否全部使用参数化查询（搜索所有 .query() / .raw() / createQueryBuilder().where() 调用）

## 输出格式
1. 未保护端点清单（路径 + 文件位置）
2. 缺少频率限制的端点清单
3. 疑似 SQL 拼接的代码位置
4. 风险评级汇总