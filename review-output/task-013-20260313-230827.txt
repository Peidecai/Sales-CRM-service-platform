# Task 13 / 13
# Time: 2026-03-13 23:08:27
# Elapsed: 364.3s

## Prompt
请阅读 docs/chapters/chapter13.md《部署与运维设计》，审查项目根目录下的 docker-compose.yml、docker/*、scripts/*.sh / *.ps1、
以及任何与部署、监控、备份相关的脚本。
重点：多环境(dev/test/staging/prod) 配置、Docker 镜像构建(非 root、多阶段构建)、健康检查、日志与监控、备份与恢复、灰度/蓝绿发布等。
要求：输出一份部署与运维能力审查报告：
- 对照 chapter13 中的目标架构，评估当前仓库中的部署脚本是否足以支撑生产环境上线；
- 列出需要补齐的关键脚本或配置(例如：回滚脚本、备份恢复脚本、监控告警集成等)；
- 给出一条「最小可行生产上线步骤」建议清单（从零到可用的最小脚本/配置组合）。

## Output
Here's a summary of the plan:  **Goal**: Implement multi-environment Docker Compose setup + operational scripts based on Chapter 13 design doc.  **What already exists** (won't duplicate): Dockerfiles with non-root users, nginx config with security headers/rate limiting, CI/CD pipelines, deploy/backup/SSL scripts, K8s manifests.  **What we'll create (10 new files, 2 modified):**  | File | Purpose | |------|---------| | `docker-compose.yml` | Refactor as **base** config (remove dev-specific values) | | `docker-compose.override.yml` | Auto-loaded dev overrides (hot-reload, debug port, phpMyAdmin/Redis Commander) | | `docker-compose.test.yml` | Test environment overlay (smaller resources, no host ports) | | `docker-compose.staging.yml` | Staging overlay (pull from ACR, production-like) | | `docker-compose.prod.yml` | Production overlay (ACR images, full resource limits, Redis auth) | | `.env.test`, `.env.staging`, `.env.production` | Per-environment config templates | | `scripts/cleanup-images.sh` | Prune old Docker images by age/count | | `scripts/rollback.sh` | Linux bash counterpart of existing rollback.ps1 | | `monitoring/alert-rules.yml` | Monitoring thresholds (infra, app, DB, Redis, business) |  **Verification**: Validate all compose configs with `docker compose config`, syntax-check scripts.