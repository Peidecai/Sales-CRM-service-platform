# Task 4 / 13
# Time: 2026-03-13 21:49:35
# Elapsed: 26.6s

## Prompt
请阅读 docs/chapters/chapter04.md《数据库详细设计》，对照 packages/server/src/modules 下的实体(Entity) 与 database/migrations 下的迁移文件。
重点检查：表名/字段命名规范(snake_case、_at 后缀)、主键/外键/索引/唯一约束、金额/时间/状态字段类型、软删除实现方式、字段默认值与非空约束等。
要求：输出一份数据库设计落地情况报告：
- 列出已经完全符合规范的关键表（如 customers/opportunities/call_records 等）；
- 列出命名或字段类型与设计不一致的表和字段，并说明可能的业务/性能风险；
- 给出一份建议的 Migration 改造清单（仅列出变更思路，不必写 SQL）。

## Output
Your message appears to be mostly garbled 鈥?the Chinese characters didn't come through correctly and are showing as `?` marks. I can only make out fragments:  - `docs/chapters/chapter04.md` - `packages/server/src/modules` 鈥?Entity + `database/migrations` - snake_case / `_at` suffixes - `customers/opportunities/call_records` tables - Migration / SQL  Could you resend your request? It looks like there was an encoding issue with the Chinese text.