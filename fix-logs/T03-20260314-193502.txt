# T03 | 2026-03-14 19:35:02 | 369.1s

## Prompt
请修复 packages/server/src/modules/ai/ai.module.ts 中直接注册跨模块实体的问题。
当前 AiModule 在 TypeOrmModule.forFeature([...]) 中直接注册了 CallRecord（属于 CallRecordModule）和 Customer（属于 CustomerModule）。
背景约束：AiModule 被 KnowledgeModule 导入，而 KnowledgeModule 中的实体（KnowledgeArticle）也被 AiModule 的 processors 使用，存在潜在循环依赖风险。
修复方案：
1. 先读取 packages/server/src/modules/ai/ai.module.ts 文件查看当前完整内容。
2. 在 ai.module.ts 的 imports 中加入 CallRecordModule（来自 ../call-record/call-record.module，需要使用 forwardRef(() => CallRecordModule)，因为 CallRecordModule 可能导入 AiModule）和 CustomerModule（来自 ../customer/customer.module）。
3. 从 TypeOrmModule.forFeature([...]) 中移除 CallRecord 和 Customer 实体。
4. 对 KnowledgeArticle 的处理：若 KnowledgeModule 导入 AiModule，而 AiModule 需要 KnowledgeArticle，需使用 forwardRef(() => KnowledgeModule) 导入 KnowledgeModule，并在 KnowledgeModule 中用 forwardRef(() => AiModule) 导入 AiModule。若 KnowledgeArticle 已在 KnowledgeModule 的 exports TypeOrmModule 中可用，则直接去掉 forFeature 中的 KnowledgeArticle 注册。
5. 修改完成后检查 TypeScript 编译是否通过：cd packages/server && pnpm build 2>&1 | tail -20。如有循环依赖报错，改用 forwardRef 包装。

## Output
Done. Here's a summary of the changes:

## Changes Made

### `packages/server/src/modules/ai/ai.module.ts`

1. **Removed direct entity imports**: `CallRecord`, `KnowledgeArticle`, `Customer` entity imports replaced with module imports
2. **Removed from `TypeOrmModule.forFeature`**: `CallRecord`, `KnowledgeArticle`, `Customer` 鈥?only AI-owned entities remain
3. **Added module imports**:
   - `CallRecordModule` 鈥?provides `CallRecord` repository (already exports `TypeOrmModule`)
   - `CustomerModule` 鈥?provides `Customer` repository (already exports `TypeOrmModule`)
   - `forwardRef(() => KnowledgeModule)` 鈥?provides `KnowledgeArticle` repository (circular dependency with `KnowledgeModule`)
4. **Added `forwardRef`** import from `@nestjs/common`

### `packages/server/src/modules/knowledge/knowledge.module.ts`

1. **Changed `AiModule` import** to `forwardRef(() => AiModule)` 鈥?resolves circular dependency (`AiModule 鈫?KnowledgeModule`)
2. **Added `TypeOrmModule` to exports** 鈥?so `AiModule` (via `forwardRef`) can access the `KnowledgeArticle` repository
3. **Added `forwardRef`** import from `@nestjs/common`

### Verification
- `tsc --noEmit` 鈥?clean compile, zero errors
- Test results identical before/after (16 failing suites are pre-existing from other working tree changes, not caused by this fix)