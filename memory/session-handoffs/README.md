# Session Handoffs

Create a handoff when work is interrupted or when the next session needs exact
continuation context.

Use `handoff.template.md` and include:

- objective
- current state
- changed paths
- commands run
- next safe action
- things not to assume
