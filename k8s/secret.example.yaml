# ⚠️  TEMPLATE — Copy to secret.yaml and fill in real values.
#     Do NOT commit secret.yaml to version control.
apiVersion: v1
kind: Secret
metadata:
  name: crm-server-secrets
  namespace: crm-sales
  labels:
    app.kubernetes.io/name: crm-server
    app.kubernetes.io/part-of: crm-sales-platform
type: Opaque
stringData:
  DB_PASSWORD: "" # Required
  JWT_SECRET: "" # Required — use: openssl rand -hex 32
  JWT_REFRESH_SECRET: "" # Required — use: openssl rand -hex 32
  DASHSCOPE_API_KEY: "" # Optional
